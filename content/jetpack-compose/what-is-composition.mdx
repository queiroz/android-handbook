---
id: "CMP-002"
title: "What is composition?"
description: "Understand what Compose keeps after your composable functions run."
difficulty: "foundational"
track: "core"
contentType: "interview-question"
estimatedTime: 6
answerTime: "~30 sec"
status: "published"
tags: [composition]
prerequisites: ["CMP-001"]
related: []
---

<ShortAnswer>

**Composition is the UI that Compose remembers between executions of your composable functions.**

Your composable functions can execute many times while a screen is alive. Compose doesn't start from nothing each time. It keeps track of the UI it already has and updates that existing UI as state changes.

That's why Composition matters. It gives the screen continuity. Without it, every execution would have to build the entire UI again from scratch.

</ShortAnswer>

## What the interviewer is testing

This question isn't really about defining the word *Composition*.

The interviewer wants to know whether you understand why Compose can execute composable functions repeatedly without treating every execution as a completely new screen.

A strong answer explains that Compose remembers the existing UI first. The runtime details come later.

## ❌ Common mistake

<CommonMistake>

> "Composition is just the first render."

Initial composition is how Compose creates the UI for the first time.

Composition is what continues to exist afterwards. It's the remembered UI that Compose maintains as the screen changes.

</CommonMistake>

## If they ask you to elaborate

Imagine an order screen.

The user opens the screen and sees a loading indicator.

A few moments later, the order arrives.

Your composable function runs again, but Compose doesn't throw away everything it already knows about that screen. It already has a remembered UI to work from. It compares the new description with what already exists and updates what needs to change.

That's why it's useful to think about two different things:

- Your composable functions describe the UI.
- Composition is the UI Compose is currently maintaining.

<Remember>

**Your code describes the UI. Composition is the UI Compose remembers.**

</Remember>

The next question is naturally:

If Compose remembers the existing UI, how does it know which parts actually need to run again? That's where recomposition comes in.

## Production thinking

The value of Composition becomes much clearer on large screens.

A production screen might switch between loading, content, errors, empty states, dialogs and refresh operations many times during its lifetime.

Compose doesn't recreate that screen from scratch every time one of those states changes. It keeps an existing UI and updates it as the application state evolves.

That's where Composition starts to matter in real applications. The more state a screen has, the more valuable that continuity becomes.

## Follow-up questions

<FollowUp>

- [What is recomposition and what triggers it?](/android/handbook/jetpack-compose/what-is-recomposition-and-what-triggers-it)

</FollowUp>

## What separates a senior answer

<SeniorAnswer>

- Explains Composition as the UI Compose remembers, not as a runtime data structure.
- Separates composable functions from the Composition they produce.
- Understands that Composition survives beyond the first execution.
- Connects Composition to why recomposition is possible.
- Avoids explaining SlotTable or other runtime internals.

</SeniorAnswer>

## Key takeaways

<KeyTakeaways>

- Composition is the UI Compose remembers between executions.
- Composable functions describe the UI; Composition maintains the current UI.
- Initial composition creates it; later updates build on it.
- Composition gives the UI continuity and prepares the ground for recomposition.

</KeyTakeaways>

## Learn more

<ReferenceList>

- [Composition lifecycle — Android Developers](https://developer.android.com/develop/ui/compose/lifecycle)

</ReferenceList>
